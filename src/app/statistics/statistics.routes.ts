import { Routes } from '@angular/router';

//component of the module
import { StatisticsComponent } from './index';

export const STATISTICS : Routes = [
    { path: 'statistics',      component: StatisticsComponent}
];
